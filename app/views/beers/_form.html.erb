<%= form_for(@beer) do |f| %>
  <% if @beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% @beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :style %><br>
      <% if @beer.new_record? %>
        <%= f.select :style, options_for_select(@styles) %>
      <% else %>
        <%= f.select :style, options_for_select(@styles, selected: @beer.style) %>
      <% end %>
    </div>
    <div class="field">
      <%= f.label :brewery_id %><br>
      <% if @beer.new_record? %>
        <%= f.select :brewery_id, options_from_collection_for_select(@breweries, :id, :name) %>
      <% else %>
        <%= f.select :brewery_id, options_from_collection_for_select(@breweries, :id, :name, selected: @beer.brewery_id) %>
      <% end %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>

<% end %>
