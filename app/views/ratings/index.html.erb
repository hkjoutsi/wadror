<h2>List of ratings</h2>

<p>To be completed...</p>

<ul>
    <% @ratings.each do |rating| %>
    <li><%= rating %> <%= link_to 'delete', rating_path(rating.id), method: :delete, data: { confirm: 'Are you sure?' } %></li>
    <% end %>
</ul>

<p>
Count of ratings: <%= @ratings.count%>
</p>

<%= link_to 'New Rating', new_rating_path %><br>
<%= link_to 'List of beers to rate', beers_path %>